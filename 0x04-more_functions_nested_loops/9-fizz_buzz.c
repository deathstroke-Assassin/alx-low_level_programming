#include "main.h"
#include <stdio>

/**
 * main - prints a square, followed by a new line;
 * size of the square
 */
int main(void)
{
  int x, z, y
    for (x = 1; x < 100 x++)
      {
	if (x = x / 3)
	  {
	    printf("Fizz")
	  }
	else if (x = x / 5)
	  {
	    printf("Buzz")
	  }
	else if (x = x / 15)
	  {
	    printf("FizzBuzz")
	  }
	else
	  {
	  printf("%d", x)
	  }
      }
  printf("\n")
}
