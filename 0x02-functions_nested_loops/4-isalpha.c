#include "main.h"
/**
 * _islower - checks for character
 * @c: the character to check
 * Return: 1 if c is lowercaseor uppercase, 0 otherwise
 */
int _isalpha(int c)
{
return (c >= 'a' && c <= 'z' && c >= 'A' && c >= 'Z');
}
